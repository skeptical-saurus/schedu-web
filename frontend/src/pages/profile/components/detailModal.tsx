import { AppointmentInformation } from 'types/appointment'

type Props = {
  appointment?: AppointmentInformation
}

const DetailModal: React.FC<Props> = ({ appointment }) => {
  return (
    <></>
  )
}

export default DetailModal